import { writable } from "svelte/store";

// export const coletores = writable([]);

// const fetchColetoresAPI = async () => {
//   const res = await fetch(`api/coletores`);
//   const data = await res.json();

//   coletores.set(data);
// };

// fetchColetoresAPI();
